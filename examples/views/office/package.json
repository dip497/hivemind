{
  "name": "@hivemind/example-view-office",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Example community view for hivemind: an isometric pixel office — every frame is a room, every tile a desk whose monitor glows with the agent's live status; click a desk to dock its live terminal.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@hivemind/tsconfig": "workspace:*",
    "@hivemind/view-sdk": "workspace:*",
    "esbuild": "0.28.0",
    "typescript": "catalog:"
  }
}
