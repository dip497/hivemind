{
  "name": "@hivemind/example-view-board",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent sessions as cards moving from doing, to review, to done — status moves them where it is certain, you decide the rest.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@hivemind/tsconfig": "workspace:*",
    "@hivemind/view-sdk": "workspace:*",
    "esbuild": "0.28.0",
    "typescript": "catalog:"
  }
}
