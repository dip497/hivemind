{
  "name": "@hivemind/example-view-orbit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Example community view for hivemind: tiles orbit their frame, coloured by agent status; click one to dock its live terminal.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@hivemind/tsconfig": "workspace:*",
    "@hivemind/view-sdk": "workspace:*",
    "esbuild": "0.28.0",
    "typescript": "catalog:"
  }
}
