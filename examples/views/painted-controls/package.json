{
  "name": "@hivemind/example-view-painted-controls",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canvas-painted workspace controls using the sandboxed view SDK.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@hivemind/tsconfig": "workspace:*",
    "@hivemind/view-sdk": "workspace:*",
    "esbuild": "0.28.0",
    "typescript": "catalog:"
  }
}
