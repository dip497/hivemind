node_modules
.pnpm-store
dist
out
build
.turbo
.cache
*.log
*.tsbuildinfo
.env
.env.local
.DS_Store
.hivemind/.agent.md
.venv/
research/runs/
.firecrawl/
test-results/
playwright-report/

# electron-builder output — large binaries, regenerated by install.sh.
dist-electron/
*.AppImage
linux-unpacked/

.rig/

# Per-developer / local dev artifacts — never committed.
# Ignore .claude/ contents (settings.local.json, agents, local skills) but SHIP
# the hive-browser skill — it's part of the Browser-tile feature, not local cruft.
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/hive-browser/
.hyperresearch/
research/
# screenshots/ are the README demo images — they MUST be committed so GitHub
# renders them (relative <img src> only works if the files are in the repo).
.preview-screenshot.png
REVIEW.md
PROPOSAL.md
PLAN-AGENTIC.md
mockup.html
