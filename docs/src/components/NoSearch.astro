---
/* Search removed deliberately. Starlight's search is Pagefind, which indexes at build time
   and so can never work in `astro dev` — the control was dead half the time anyone looked
   at it. With `pagefind: false` in astro.config.mjs nothing is indexed either, so the
   header keeps no promise it cannot make. */
---
