Built plugins the e2e suite runs against: the host is what is under test, so these stay fixed.
Their source is in the plugins repository (dip497/hivemind-plugins); refresh by copying a
`views/<name>/dist` from there, with the id unscoped.
